#include <iostream>
#include <map>
#include <vector>
#include <deque>
#include <set>
#include <functional>
#include <cmath>
#include <algorithm>
#include <numeric>
#include <stack>

using namespace std;

#define IO ios::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr)
#define fi first
#define se second
#define endl "\n"
#define int long long
#define dcmp(a) (a < -eps ? -1 : a < eps ? 0 : 1)
#define sz(x) (int) x.size()
#define alls(v) v.begin(), v.end()
#define ralls(v) v.rbegin(), v.rend()
#define vi vector<int>
#define vvi vector<vector<int>>
#define vb vector<bool>
#define vs vector<string>
#define vp vector<pair<int, int>>
using ll = long long;


typedef pair<int, int> PII;

const int mod = 998244353;
const int INF = 0x3f3f3f3f;
const double eps = 1e-8;
const int N = 1e6 + 10;

void solve()
{
    cout << "hello wrold";
}

signed main()
{
#ifdef LOCAL
    freopen("B:/code/CODE_Cpp/ACM/in.txt", "r", stdin);
    freopen("B:/code/CODE_Cpp/ACM/out.txt", "w", stdout);
#endif
    IO;
    int t = 1;
    //cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}
